const medusaRotate = document.getElementById("medusa-rotate");

document.addEventListener("mousemove", (event) => {
  console.log("mouse moves");

  //   medusaRotate.style.border = "1px solid white";

  // image.style.transform = `translate(${offsetX}px, ${offsetY}px) rotate(${offsetX / 5}deg)`;
});
